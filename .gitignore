# Dependencies
node_modules/

# Gatsby
.cache/
public/

# Build
build/

# Env — never commit
.env*

# Amplify — never commit
# The modeler is a frontend-only consumer; the portal owns the backend.
# The amplify/ directory is only created locally by `amplify pull` to generate
# aws-exports.js and GraphQL operations. It must never be committed here.
amplify/
src/aws-exports.js
.graphqlconfig.yml

# Real SmartHome instances — contain personal address data, keep local only
# Demo instances (*-DEMO-*.ttl) are safe to commit
semantic-core/instances/DE-[0-9]*.ttl
semantic-core/instances/FR-[0-9]*.ttl
semantic-core/instances/BE-[0-9]*.ttl

# IDE
.vscode/

# OS
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

#amplify-do-not-edit-begin
amplify/\#current-cloud-backend
amplify/.config/local-*
amplify/logs
amplify/mock-data
amplify/mock-api-resources
amplify/backend/amplify-meta.json
amplify/backend/.temp
build/
dist/
node_modules/
aws-exports.js
awsconfiguration.json
amplifyconfiguration.json
amplifyconfiguration.dart
amplify-build-config.json
amplify-gradle-config.json
amplifytools.xcconfig
.secret-*
**.sample
#amplify-do-not-edit-end
